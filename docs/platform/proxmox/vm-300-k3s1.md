# VM 300 – k3s1.brownrook.net

## Goal
Provision VM 300 (RHEL 10.1) from template 9100 with deterministic:
- hostname/FQDN
- static IP (192.168.2.230)
- DNS = 192.168.2.250 (unbound)
- SSH: password auth enabled temporarily; root login disabled

## Inputs
- Template: VMID 9100 (rhel10-cloud-template)
- Snippet: /var/lib/vz/snippets/300-userdata.yaml
- Storage: local-lvm (lvmthin), snippets on local (/var/lib/vz)

## Provisioning steps (repeatable)
Run on pve:
1. `scripts/pve/30-clone-k3s1-300.sh`

## Verification
On k3s1:
- `hostnamectl`
- `ip addr show eth0`
- `cat /etc/resolv.conf`
- `sudo cloud-init status --long`
- `sudo sshd -T | egrep 'passwordauthentication|permitrootlogin'`

From Mac:
- `dig +short k3s1.brownrook.net @192.168.2.250`
- `ssh paul@k3s1.brownrook.net`