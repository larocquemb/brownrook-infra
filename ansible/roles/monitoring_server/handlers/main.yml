---
- name: Reload Prometheus
  ansible.builtin.systemd_service:
    name: "{{ monitoring_server_prometheus_service }}"
    state: reloaded

- name: Restart Grafana
  ansible.builtin.systemd_service:
    name: "{{ monitoring_server_grafana_service }}"
    state: restarted
